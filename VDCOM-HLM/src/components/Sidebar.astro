<aside>
    <h2>IPS Hospital Local de Montelíbano</h2>
    <nav class="nav-menu">
        <ul>
            <li>
                <a href="index.html" class="active"
                    ><span class="icon">🔍</span> Búsqueda Estudios</a
                >
            </li>
            <li>
                <label for="dicom-file-input" style="cursor:pointer; display:flex; align-items:center;">
                    <span class="icon">👥</span> Cargar
                </label>
                <input id="dicom-file-input" type="file" accept=".dcm,application/dicom" style="display:none" />
            </li>
            <li>
                <a href="#" data-page="settings"><span class="icon">⚙️</span> Configuración</a>
            </li>
        </ul>
    </nav>
    <div class="sidebar-footer">
        <a href="#" class="logout">Cerrar Sesión</a>
    </div>
</aside>

<style>
    aside {
        width: 280px;
        background: linear-gradient(
            180deg,
            #ffffff 0%,
            #f8f9fa 50%,
            #e9ecef 100%
        );
        padding: 0;
        box-shadow: 4px 0 20px rgba(0, 0, 0, 0.1);
        display: flex;
        flex-direction: column;
        border-right: 1px solid #e0e6ed;
        overflow: hidden;
    }

    aside h2 {
        padding: 24px 24px 16px;
        color: #1a1a1a;
        font-size: 20px;
        font-weight: 700;
        text-align: left;
        border-bottom: 1px solid #e9ecef;
        margin: 0;
        background: rgba(255, 255, 255, 0.8);
    }

    .nav-menu {
        flex: 1;
        padding: 8px 0;
    }

    aside ul {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    aside li {
        margin: 0;
    }

    aside a {
        display: flex;
        align-items: center;
        padding: 16px 24px;
        color: #495057;
        text-decoration: none;
        font-size: 16px;
        font-weight: 500;
        transition: all 0.2s ease;
        border-left: 4px solid transparent;
    }

    aside a:hover {
        background: #e3f2fd;
        color: #1976d2;
        border-left-color: #2196f3;
    }

    aside a.active {
        background: #e3f2fd;
        color: #1976d2;
        border-left-color: #2196f3;
        font-weight: 600;
    }

    aside .icon {
        margin-right: 12px;
        font-size: 20px;
    }

    aside-footer {
        padding: 16px 24px;
        border-top: 1px solid #e9ecef;
        background: rgba(248, 249, 250, 0.8);
    }

    .logout {
        display: block;
        color: #dc3545;
        text-decoration: none;
        font-size: 14px;
        padding: 8px 0;
        transition: color 0.2s;
    }

    .logout:hover {
        color: #c82333;
        text-decoration: underline;
    }
</style>
